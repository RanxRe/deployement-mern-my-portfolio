{
  "name": "portfolio",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
  "start": "node server.js",
  "build": "cd client && npm install && npm run build",
  "test": "echo \"Error: no test specified\" && exit 1",
  },
  "author": "Ranjeet Kumar Mahato",
  "license": "ISC",
  "description": "Simple backend for my-portfolio",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "nodemailer": "^7.0.6",
    "nodemailer-sendgrid-transport": "^0.2.0"
  }
}
